name: shake_event
description: A shake event listener for Android and iOS. You can listen to shake events and set the threshold to define how precisely you want to listen to these events.
version: 0.0.9
homepage: https://github.com/iatsu/shake_event

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.26.0
  sensors: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: