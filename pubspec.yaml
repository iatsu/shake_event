name: shake_event
description: A cross-platform shake event listener.
version: 0.0.6
homepage: https://github.com/iatsu/shake_event

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.26.0
  sensors: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: